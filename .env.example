# Copy to .env and fill in for your machine.
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=https://api.example.com

# Local database used by tooling/scripts (not read by the Vite app itself).
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=backend
DB_USERNAME=root
DB_PASSWORD=
